# Para leer una cadena o un número en una línea
#linea = sys.stdin.readline().strip()
#numero = int(sys.stdin.readline())

# Para leer dos números de una misma línea (ej: "5 100")
#n, k = map(int, sys.stdin.readline().strip())

#Listas
#mi_lista = [0] * n
#Agregar: mi_lista.append(valor)
#Quitar: mi_lista.pop(valor)

# Leer una lista de números de una línea
#numeros = list(map(int, sys.stdin.readline().strip().split()))

# Crear una lista de los cuadrados de esos números
#cuadrados = [x**2 for x in numeros]

#Strings
#Subcadena: s[i:j]
#Invertir: s[::-1]

#Normalizar minúsculas y mayúsculas:
#s.lower()
#s.upper()

#Prints
#print(f"El resultado es {variable}")

#Frecuencia:
# Contar la frecuencia de cada carácter en un string
#frecuencias = {}
#for char in "abracadabra":
#    frecuencias[char] = frecuencias.get(char, 0) + 1
# Resultado: {'a': 5, 'b': 2, 'r': 2, 'c': 1, 'd': 1}

#Otra opción para frecuencia con dicciontarios:
#frecuencias = Counter("abracadabra")

#Conjuntos(set) elementos únicos
#lista_con_duplicados = [1, 2, 2, 3, 4, 4, 4]
#elementos_unicos = set(lista_con_duplicados) 



